meta {
  name: "Get Student Attendance Log"
  type: "http"
  seq: 5
}

get {
  url: "{{base_url}}/api/classes/:classId/members/:studentId/attendance-log"
  body: "none"
  auth: "bearer"
}

params:path {
  classId: "{{class_id}}"
  studentId: "{{student_id}}"
}

headers {
  Content-Type: "application/json"
}
