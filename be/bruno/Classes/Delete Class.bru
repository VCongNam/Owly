meta {
  name: Delete Class
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/classes/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: ID_CUA_LOP_HOC_TREN_DB
}
