meta {
  name: Get Grade Categories
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/classes/:classId/grade-categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

params:path {
  classId: CLASS_UUID
}
